# Database configuration
MONGODB_URI=mongodb://localhost:27017/maison_de_ffm

# JWT configuration
JWT_SECRET=supersecretkey_dev_only   # ⚠️ Change this in production

# Environment
NODE_ENV=development

# Email configuration (optional, for notifications or auth)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM="Maison De FFM <noreply@maisondeffm.com>"

# Server configuration
PORT=5000
